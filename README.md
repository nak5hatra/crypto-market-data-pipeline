# crypto-market-data-pipeline
Production-style crypto market data pipeline built using Python, Polars, and PostgreSQL.  The pipeline extracts data from the CoinGecko API, performs transformations, and loads it into a relational database for analytics.
